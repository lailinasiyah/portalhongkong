<IfModule mod_rewrite.c>
    RewriteEngine On

    # Jangan rewrite file & folder yang ada
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d

    # Jangan loop ke public
    RewriteCond %{REQUEST_URI} !/public/

    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
